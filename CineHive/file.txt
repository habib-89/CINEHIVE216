Double-click `start-cinehive.cmd` to launch both the backend and frontend.
Use `start-backend.cmd` or `frontend/start-frontend.cmd` when you need only one.

API base URL: http://localhost:3000/

Before starting, create `CineHive/.env` from `.env.example` and provide valid local Oracle credentials.
Never commit credentials, connection strings containing passwords, or JWT secrets.
